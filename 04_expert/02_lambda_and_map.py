numbers = [1, 2, 3, 4, 5]  # A list of numbers
squared = list(map(lambda x: x ** 2, numbers))
# Uses a lambda function to square each number in the list.
print(squared)
# Prints the list of squared numbers.
