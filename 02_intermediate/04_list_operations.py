numbers = [1, 2, 3, 4, 5]# A list of numbers
doubled_numbers = [n * 2 for n in numbers]
# Creates a new list by doubling each number in the original list.
print(doubled_numbers)
# Prints the new list of doubled numbers.